# Web Agent Environment Variables
# Copy this file to .env and fill in your API keys
#
# Usage:
#   cp .env.example .env
#   # Edit .env with your API keys
#   docker compose up -d

# =============================================================================
# Required API Keys
# =============================================================================

# Anthropic Claude API key (for LLM)
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# VoyageAI API key (for embeddings)
VOYAGE_API_KEY=your_voyage_api_key_here

# GitHub personal access token (for issue search/creation)
# Requires: repo scope for private repos, or public_repo for public repos
GITHUB_TOKEN=your_github_token_here

# =============================================================================
# GitHub Repository Configuration
# =============================================================================

# GitHub repository owner (default: alphapaca)
GITHUB_OWNER=alphapaca

# GitHub repository name (default: ClaudeClient)
GITHUB_REPO=ClaudeClient

# =============================================================================
# LLM Configuration
# =============================================================================

# Claude model to use (default: claude-sonnet-4)
# Options: claude-sonnet-4, claude-opus-4, claude-haiku-3
LLM_MODEL=claude-sonnet-4

# Maximum tokens for LLM response (default: 4096)
MAX_TOKENS=4096

# Temperature for LLM response (default: 0.7)
# Lower = more deterministic, Higher = more creative
TEMPERATURE=0.7

# Maximum agent iterations for tool use loops (default: 10)
MAX_AGENT_ITERATIONS=10

# =============================================================================
# Server Configuration
# =============================================================================

# Server port (default: 8080)
PORT=8080

# Auto-index codebase on startup if database is empty (default: true)
AUTO_INDEX=true
